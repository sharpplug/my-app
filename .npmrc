# react-day-picker@8 declares a peer dependency on react<=18 while this app
# is on React 19 - it works fine at runtime, but a plain `npm install`
# hard-fails on the ERESOLVE conflict without this flag.
legacy-peer-deps=true
